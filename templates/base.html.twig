<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="author" content="Claire VERDIER">
    <meta name="description" content="un mini-site listant des produits">
    <meta name="keywords" content="produits, chaussures, t-shirts, pulls, e-commerce">
    <link rel="icon" type="image/png" href="{{ asset('build/images/logo.png') }}"/>
    <title>{% block title %}Welcome!{% endblock %}</title>
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}
</head>
<body>
    {% include '_navbar.html.twig' %}
    <main>
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}
        {% block body %}{% endblock %}
        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </main>
</body>
</html>
